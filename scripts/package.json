{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "test": "node --import tsx --test src/shift-sync.test.ts",
    "check-shifts": "tsx ./src/verify-shift-sync.ts",
    "sync-shifts": "tsx ./src/sync-shifts.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
