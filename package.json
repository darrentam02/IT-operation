{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm run build:production",
    "build:production": "PORT=21727 BASE_PATH=/ pnpm --filter @workspace/it-operations-control-tower run build && pnpm --filter @workspace/api-server run build",
    "start": "PORT=\"${PORT:-8080}\" node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "test": "vitest run",
    "smoke:dashboard": "node ./scripts/smoke-dashboard.mjs",
    "test:jira-sync": "node --test src/services/jiraSync.test.js",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "@supabase/supabase-js": "^2.112.4",
    "cohere-ai": "^8.1.0",
    "dotenv": "^17.4.2",
    "openai": "^7.8.0",
    "pdf-parse": "^2.4.5",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "jsdom": "^26.1.0",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@11.24.0"
}
